[package]
name = "freeremote_ffi"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "freeremote_native"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
freeremotedesk = { path = "../..", default-features = false }
