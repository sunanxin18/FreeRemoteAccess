[package]
name = "frd-icon-assets"
version = "0.1.0"
edition = "2021"

[dependencies]
ico = "0.4"
image = { version = "0.25", default-features = false, features = ["jpeg", "png"] }

[dev-dependencies]
tempfile = "3"
