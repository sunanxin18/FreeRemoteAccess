# RDP EGFX/H.264 验证记录（2026-09-07；2026-09-08 continuation）

## 2026-09-08：末符号结构与 Windows ARM64 工具链定位

`07ce52e` 增加静态 publisher operation/detail，全RDP288项通过。
随后clean release probe再次走到ProgressiveEntropy，首帧1125ms、ClearCodec3次、
queued/runtime各2帧，20秒后正常joined；不是持续刷新通过。
本地临时诊断构建进一步确认首个失败分量为完整4096零游程（消费31bit），
随后还有一个完整负号/GR(0)末符号与6bit零padding；原decoder把末符号当padding。
该结构将通过独立合成fixture复现，临时原始分量只保留在忽略的本地0600文件中，
不提交、不上传、不打印内容。修复必须保留short尾并严格拒绝多余字节和非法填充。
随后严格消费一个完整末符号的修复通过合成RED/GREEN、独立FreeRDP C decoder
与本地分量的输出一致性验证；RDP全289 passed/3 ignored、cargo fmt检查通过。
此处只是该失败分量的修复验证，完整真实会话结果需另行记录。

Windows run `34185963994` 的三个package job均成功，但ARM64 native runtime
job `101939738206` 在编译ring时失败：clang GNU driver不接受继承的
`/clang:-fgnuc-version=4.2.1`。`357a5bc` 改用两个driver都接受的
`-Xclang -fgnuc-version=4.2.1`；本地clang/clang-cl的真实AArch64 inline asm及
ring SHA256汇编均交叉编译为ARM64 COFF。完整SDK/native gate仍需新CI验证。

## 2026-09-08：独立 RLGR1 oracle 与更早的发布错误

`6c8533e` 增加 pinned FreeRDP C encoder 生成的非零4096系数合成fixture，
本机重新编译C oracle并生成2269字节后逐字节一致；entropy专项9项通过。
该fixture不含远程会话数据，也不证明Windows的终端zero-run语法。

为定位此前 `entropy trailing data` 构建了仅供本地诊断的首个失败分量采集版。
两次有界授权连接均在更早的 `Publisher` 错误停止：ClearCodec成功2次，
queued/runtime各2帧，1个baseline、480patch、7,372,800bytes；reset为3次，
首帧分别1080ms和1151ms。Progressive及AVC成功计数仍为0。
两轮均20秒后Disconnect并joined，exit1；未生成压缩分量文件。
临时采集代码已从工作区移除，不进入提交。当前先补发布失败的精确静态原因，
不能从ClearCodec计数直接推断触发错误的是哪一种EGFX命令。

## 2026-09-08：Progressive 已接线，首次真实 RLGR 尾部被拒绝

`2fddb5d` 将 native Progressive 接入真实 EGFX Start/End callback、WTS2、离屏存储、
DeleteEncodingContext/surface 生命周期；RDP全285tests通过。实际1080p 510tile输入合并为
1个无洞patch，原256个更新与64MiB发布上限保持不变。

两次有界授权AVC444 probe均确认V10.7 flags0x82；ClearCodec3位图、2帧runtime接收、
1完整baseline、480patch、7,372,800bytes；Progressive与AVC成功计数仍0。第一轮首帧1008ms，
错误ProgressiveEntropy；补充静态原因后第二轮首帧1086ms，明确为 `entropy trailing data`。
两轮均20秒主动Disconnect且worker joined，exit1 `probe_egfx_pipeline_failed`。
这证明收到实际Progressive且走到新entropy层，但不证明Progressive解码或持续刷新成功。

进一步审计发现终端zero-run符号规则与本地IronRDP encoder有差异，正在用独立FreeRDP
RLGR1编码参考建立失败fixture；禁止直接忽略多余数据或恢复截断补零。

## 2026-09-08：真实 ClearCodec 首帧与 codec 计数

`3d0746b` 增加实际 decode/queued/runtime 累计计数，并以 V10.7 `SCALEDMAP_DISABLE` 准确声明未实现的缩放映射。授权 probe 确认 V10.7 flags `0x82`，指纹匹配；ClearCodec bitmaps=3、frames queued=2、runtime accepted=2；完整 baseline=1、patches=480、decoded_bytes=7,372,800、first_frame_ms=1087。首末帧仅相隔 1 ms，20 秒观察后主动断开并 joined，不能称为持续刷新成功。

AVC420 pictures=0、AVC444 updates=0；后续 `UnsupportedCodec 0x0009` 仍令 EGFX 停用。此结果只证明本机 ARM64 SIMD ClearCodec 到 runtime 的真实首帧，不证明 GUI 呈现、其他平台运行或 H.264 首帧。探针原退出判定只检查有帧，故本次进程 exit=0 不代表 AVC 验收；后续已补判定，AVC opt-in 若请求 codec 实际计数为零或曾发生 pipeline failure，必须退出失败，三项 example tests 通过。

Progressive 容器解析新增严格 blockLen、tileDataSize、嵌套 tile 精确消费与数量/输入预算，六项 focused tests 通过。状态/熵解码/SIMD 正在独立实现，未接线生产。

## 2026-09-08：离屏表面修复后遇到 RemoteFX Progressive

提交 `706084f` 实现离屏 backing、有效区域、映射/重映射及 fill/copy/cache/evict；RDP 全量 235 passed、2 ignored，独立审核通过。内存分别限制为 persistent 256 MiB、scratch 256 MiB、publication 64 MiB，不代表整个进程上限。release probe 构建通过。

复用原指纹的授权 Windows AVC444 opt-in 探针完成 60 秒有界观察并主动断开、worker joined：V10.7 (`0x000A0701`) 确认 AVC420/AVC444；原 `PublisherUnmappedSurface` 不再出现，首次失败改为 `UnsupportedCodec`、codec `0x0009`。最终 resets=2、frames=0、full_baselines=0、patches=0、decoded_bytes=0、`probe_no_decoded_frames`。这不是首帧或 H.264 解码成功证据。

[MS-RDPEGFX WireToSurface2](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-rdpegfx/49ccafc7-e025-4293-9650-dcae1b7b9e84) 定义 `0x0009` 为 RemoteFX Progressive；其持久 codecContextId 与传统 RemoteFX 不可混用。后续必须补全实际发送路径或取得规范允许且真实验证的协商方案，不能忽略该 PDU 后宣称成功。生产仍 LegacyOnly；scaled/window mapping 与跨帧首次完整 baseline 聚合仍待验证。


## 2026-09-08 Windows ARM64 原生解码通过

Windows run `34182980455` 整体成功，ARM64 native runtime job `101929312094` 在真实 ARM64 Windows 上重新验证下载的同次package，并加载随包DLL通过全部6项fixture。连同Windows x86/x64、Linux三架构及macOS ARM64已有记录，固定FFmpeg plugin构建/目标解码门禁已满足；新ClearCodec及真实RDP AVC仍独立未完成。

## 2026-09-08 Windows ARM64 包构建成功

Windows run `34182980455` 的 ARM64 package job `101925633226` 已成功完成完整 AAC、GUI release、PE/package verifier 和上传。ARM64 应用 artifact 为 22,664,457 bytes，对应源码为 11,729,109 bytes。该结果证明目标构建和包检查，后续 `Windows ARM64 native decoder runtime` 尚待依赖 job 完成，不能提前宣称实际 ARM64 DLL 解码通过。

## 2026-09-08 真实定位：映射前 surface 更新

`35fdfb5` 的有界真实 probe 将首次失败精确定位为 `PublisherUnmappedSurface`：服务器在映射前发送 ClearCodec，原发布器的 mapped 前置条件拒绝了合法离屏更新。会话确认 V10.7，60秒无帧后主动断开且cleanup=joined。下一修复是受总预算限制的surface backing/有效区域保存与Map发布，不是放宽矩形检查或跳过序号。

## 2026-09-08 最新真实接线与 ARM64 AAC 结果

`ec5e3c3` 显式 AVC444 probe 再次确认 V10.7/AVC 能力，进入 ClearCodec 路径后 `first_failure=Publisher`，未处理编码计数为0；60秒内无首帧，主动断开且cleanup=joined。故不能把离线225测试通过视为真实首帧通过；正在细分surface布局/发布约束，未证实具体原因前不归咎服务端。另Windows run34182980455的ARM64完整AAC编译gate已成功，GUI build仍在执行。

## 2026-09-08 ClearCodec / NSCodec 接线本地验证

ClearCodec 与完整 NSCodec SIMD provider 已接入显式 EGFX publisher；会话共享 sequence/cache，BGRA 直接进入 BGRX patch，纯缓存重置不伪造像素，失败及发布溢出清空 generation。完整 RDP 225 项测试通过，NSCodec 包含微软官方 15×10 全像素示例。真实首帧、跨目标运行和性能仍待验证；生产默认 LegacyOnly 保持。

## 2026-09-08 ClearCodec 基础状态层验证

ClearCodec 严格状态层、会话级序号/缓存事务与 SIMD 像素内核已注册编译：25 项 ClearCodec/kernel 测试、完整 RDP 214 项通过。含 residual 前缀、bands 位域、RLEX、glyph/cache、零子区域与覆盖元数据预算回归。NSCodec 实际后端和 EGFX 接线仍在开发，因此未打开生产门禁。

## 2026-09-08 Windows x64 完整包门禁通过

Windows run `34181521241` 已结束：x64 job `101921418412` 通过 6 项解码 fixture、GUI/package gate、30/30 Pester 与产物上传；x86 job 同样成功。x64 应用 artifact 为 23,582,300 bytes。ARM64 AAC 编译失败，后续 ARM64 native runtime job 因依赖失败而 skipped，不能算通过。

## 2026-09-08 Windows x86 包与原生解码通过

Windows run `34181521241` 的 x86 job `101921418321` 已成功：真实 i686 进程 6 项 decoder fixture、GUI release、staging/package verifier 与产物上传全部通过。应用包 `freeremotedesk-windows-package-x86` 为 22,263,948 bytes，对应源码包 11,729,109 bytes。此结果不是 Windows GUI 真机交互或 RDP AVC 首帧证据；同轮 ARM64 失败和 x64 仍在运行分别保留。

## 2026-09-08 明确未支持编码与 ARM64 AAC 构建修复

未支持的 WireToSurface1 codec 现在以固定 `UnsupportedCodec` 终止当前 EGFX generation，清空旧队列并保留数值诊断；189 项 RDP 测试通过，不宣称 legacy 恢复。Windows ARM64 job `101921418195` 的 GUI 构建失败于 FDK 未识别 MSVC ARM64而进入 `#warning` 分支；目标限定 clang-cl/GNU兼容模式已验证真实 AArch64 COFF 汇编探针，完整 FDK 编译由新增 CI gate 验证，尚未通过。

## 2026-09-08 V10.7 AVC 能力确认，仍发送 ClearCodec

同一 `20f9f69` 运行现有 AVC444 opt-in provider 后，服务器确认 `0x000A0701`（V10.7）、flags `0x2`，诊断 `avc420=true avc444=true`；但 60 秒内仍发送至少 202 个当前未处理的 ClearCodec（0x8）记录，最终零帧、`probe_no_decoded_frames`，主动断开并 `cleanup=joined`。这补充了 AVC 能力协商成功证据，但不是 AVC 解码首帧证据；高版本广告不能替代 ClearCodec 兼容路径。

## 2026-09-08 无首帧定位：V8.1 / ClearCodec

`20f9f69` 的第二次有界 opt-in probe 确认服务器选中 `version=0x00080105`（V8.1）、`flags=0x2`（没有 AVC420_ENABLED），随后收到 `codec_id=0x8`（ClearCodec）；未处理计数至少 67，仍无像素。由此证明本次无画面来自已协商 EGFX 的 ClearCodec 未处理路径，而不是 H.264 解码耗时。该证据不证明服务器永远不支持 H.264；后续需独立处理能力协商/服务器策略与已选编码的兼容性，不能默默忽略编码并将连接判为成功。

## 2026-09-08 新真实连接：EGFX 确认但没有首帧

在 `d5140a6` 上使用此前授权的凭据，通过关闭回显的 stdin 管道运行 AVC420 opt-in probe，使用现有 pin 目录和签名 ARM64 app bundle。指纹匹配、认证/激活成功；`start_calls=1 capability_messages_queued=1 typed_confirmation_ever=true egfx_confirmed=true avc420=false avc444=false failure_count=0`。服务器触发三个 1280×720 generation reset，但 60 秒总时限内 `frames=0 full_baselines=0 patches=0`，最终 `probe_no_decoded_frames`，主动 Disconnect 后 `cleanup=joined`。因此现在已证明 EGFX CREATE/能力确认到达，但尚无 AVC 确认或首帧；不能沿用此前“仅未确认”的推测，更不能打开生产门禁。下一项是确认服务器选中的版本/flags及未出帧原因。

## 2026-09-08 EGFX 阶段诊断

新增 `start_calls`、`capability_messages_queued`、`typed_confirmation_ever`、`failure_count` 和固定 `first_failure` 分类。能力排队不等于网络发送；确认闩锁在后续失败后保留，只认可可解析的确认版本。诊断不保存载荷、凭据或上游错误原文。RDP 全套 187 项测试、probe examples 编译与格式检查通过，覆盖从未确认与确认后失败的区别。生产默认 `LegacyOnly` 未改变。

本次 TCP 探测确认已保存 RDP 目标可达，但钥匙串读取超时，未开展新的登录；以上诊断尚无新真实会话证据。若 start 失败导致上游移除 DVC，当前活动通道快照可能无法导出该最终计数；不能将缺少阶段数据推断为服务器拒绝。

## 2026-09-08 Linux 三架构目标解码通过

[Linux run 34181521242](https://github.com/sunanxin18/FreeRemoteAccess/actions/runs/34181521242) 在 `ca8628e` 上三个架构全部成功：x86_64 原生、i686 32 位目标进程、ARM64 原生各通过 11 项 plugin 单元测试与 6 项随包解码 fixture，覆盖 AVC420、AVC444 和 HEVC 离线样本。ELF/依赖/路径/ABI 与产物上传检查通过。该结果补齐 Linux 目标解码执行证据，仍不证明 Linux GUI 或真实 RDP 编码互操作。

## 2026-09-08 Windows x64 包验证与测试失败边界

Windows run `34180014676` 的 x64 job `101917040083` 已完成 GUI release 构建、staging 与精确 package verifier。最后 Pester 为 29 passed / 1 failed：旧 EXE 替换测试写入短文本，新增 PE Machine 校验提前以“PE 文件过短”拒绝，导致原有 payload hash mismatch 断言未命中。必须修正样本以保留有效 PE 头并改变内容，再复跑原有哈希拒绝断言；不能据此将整个 package job 记为成功，也没有成功上传该包。

## 2026-09-08 ARM64 macOS 托管包完成

[macOS run 34180014775](https://github.com/sunanxin18/FreeRemoteAccess/actions/runs/34180014775) 在 `c15660b` 上成功完成 ARM64 原生测试、FFmpeg 构建、应用 staging/verifier 和上传。API 核实产物 `freeremotedesk-macos-arm64`（24,948,553 bytes）与对应 FFmpeg 源码（11,724,620 bytes）均未过期。这是构建和离线解码/包验证证据，不扩大真实 RDP 编码互操作范围。macOS 后续仅支持 ARM64。

[CI run 34180529670](https://github.com/sunanxin18/FreeRemoteAccess/actions/runs/34180529670) 在 `50dc4f4` 上完成 Rust formatting、macOS ARM64 与 Ubuntu 编译/安全测试，全部成功。独立 EGFX crate 现在使用提交的锁文件，先 `cargo fetch --locked` 再 `cargo test --frozen`；此前干净 runner 缺少依赖的问题已通过托管复跑验证。

同轮 Windows run `34180014676` 的 x86/ARM64 FFmpeg 构建通过，但 host safe suite 在 `main444_decode.rs:709` 查找 x64 默认 bundle 失败；这两个包仍未完成验收，必须修复测试架构对应关系并补充目标运行证据。

## 2026-09-08 托管 CI 新证据（取代下文 Linux 未执行状态）

[Linux FFmpeg run 34179144122](https://github.com/sunanxin18/FreeRemoteAccess/actions/runs/34179144122) 在提交 `6b5191f04e0d891e16063efe93ce04cf1dc05e9f` 上完成：x86_64、i686、AArch64 三个 bundle 的构建、ELF/依赖/路径/ABI 导出检查与产物上传均成功，三个对应源码产物也已上传。x86_64 的原生 fixture 与插件加载/ABI 调用通过；i686/AArch64 的加载步骤明确跳过，host-side tests 不构成目标架构运行证据。Linux GUI、目标机交互和 live RDP 尚未验证，因此平台整体仍为 `受限验证`/`开发中`。

该运行修复了 YAML 折叠 shell 续行，以及 FFmpeg configuration 嵌入临时 `--prefix` 的真实路径泄露；现使用固定 `/usr` 加 `DESTDIR` staging，未放宽 verifier。Windows ARM64 已改固定 LLVM-MinGW，Windows x86 使用静态 libgcc，实际包结果仍待新 CI。macOS Intel CI 暴露的负色度 SIMD 乘加错误已在 `53d10de` 修复，Rosetta 完整 RDP 186 项通过，ARM64 YUV 回归 7 项通过；此前本机 ARM64 通过不能代表 Intel 正确性。

本记录只描述当前工作区已经执行的检查和仍然关闭的生产门禁。编译、单元测试、
FFmpeg ABI 合同和离线选择器测试不等于 RDP EGFX 真实互操作。Windows/macOS 组合根
现在会在固定 FFmpeg bundle 成功加载且 AVC420 能力精确匹配时注入 EGFX provider；
bundle 缺失或能力不匹配时仍保持 Bitmap/RemoteFX legacy-only。2026-09-08 的
macOS arm64 bundle 已重新构建为 H.264+HEVC decoder/parser，并通过包签名、架构、依赖、
ABI、3 项原生 HEVC fixture、1 项原生 AVC420 fixture、1 项原生 AVC444 fixture 和 codec verifier；授权 Windows 目标上的 opt-in probe 成功加载本地 AVC420 provider，
但没有产生可归因于 EGFX 的第二个 `ResetGraphics`/EGFX surface 更新，因此真实 EGFX
首帧、持续刷新和恢复门禁仍关闭；生产组合根即使加载精确匹配的签名 bundle，也默认使用 `LegacyOnly`，不广告 AVC420；
只有显式启用的有界互操作探针可以请求 EGFX。

2026-09-08 在提交 `cec575f` 上完成了一次 legacy-only 有界 Windows RDP 回归：
1280×720 首个完整 baseline、20 秒内 169 个 frame boundary/169 个 patch/2 个
FullBaseline，随后显式断开并正常回收。该探针没有注册 EGFX decoder provider，因而只
证明 Bitmap/RemoteFX fallback 仍可用；它不提供 RDPGFX `CapabilitiesConfirm`、AVC420、
AVC444 或 HEVC 的服务器互操作证据。

## 标准 RDP HEVC wire profile 复核

2026-09-08 对照公开的 [MS-RDPEGFX `RDPGFX_WIRE_TO_SURFACE_PDU_1` codec ID 表](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-rdpegfx/fb919fce-cc97-4d2b-8cf5-a737a00ef1a6)，当前标准表包含 Uncompressed、RemoteFX/CAVIDEO、ClearCodec、Planar、AVC420、Alpha、AVC444 和 AVC444V2，但没有 HEVC codec ID。微软的 [Azure Virtual Desktop 图形编码说明](https://learn.microsoft.com/en-us/azure/virtual-desktop/graphics-encoding) 提到部分 GPU 虚拟机产品场景使用 HEVC，却没有公开对应的 MS-RDPEGFX `WireToSurface1` 编号、能力版本、NAL/参数集封装或恢复规则。微软在 [2025 RDP Interoperability Lab 更新材料](https://learn.microsoft.com/en-us/openspecs/interopevents/interopevents/media/index/pdfs/rdp%20updates_rdp_io_lab_2025.pdf) 中也把硬件 HEVC 编解码列为 Windows 365/Azure Virtual Desktop 产品能力；该材料同样没有给出本客户端所需的 RDPGFX codec ID 或 payload 结构。

因此 Azure 产品说明不能替代当前客户端所需的 RDP wire evidence。`HEVC` 选择器仍只是协议中立、失败关闭的规划接口；在获得精确产品协议或授权抓包并完成首帧、持续刷新、恢复验证前，connector 不广告 HEVC，也不把本地 FFmpeg HEVC fixture 当作 RDP HEVC 互操作证明。

上游 IronRDP 当前 [README 的客户端图形解码列表](https://github.com/Devolutions/IronRDP#graphics)
仍只列出 raw bitmap、Interleaved RLE、RDP 6 bitmap 和 RemoteFX，EGFX 作为额外的 PDU
原语提供；其 [AVC444 blank-screen issue](https://github.com/Devolutions/IronRDP/issues/1563)
记录了在没有 AVC444 decoder 时默认广告 V10.7，Windows 选择 AVC444 后整帧丢失的实测。
这也是本项目要求 capability 广告必须与已加载 decoder 精确匹配、AVC444 provider 默认不接线的
直接上游依据。

## EGFX AVC420/AVC444 bounded probe 接线

`crates/frd-protocol-rdp/examples/bounded_live_probe.rs` 现在保留默认
legacy-only 行为，并增加了显式环境变量 opt-in。设置
`FRD_RDP_PROBE_EGFX_AVC420=1` 或 `FRD_RDP_PROBE_EGFX_AVC444=1`（两者不能同时设置），
再设置 `FRD_RDP_PROBE_MACOS_APP=/absolute/path/FreeRemoteDesk.app` 后，probe 才会从指定
的 macOS `.app` 加载 FFmpeg backend，复用签名、包内路径、固定 FFmpeg 版本和 ABI 校验。
AVC420 模式注入精确的 H.264/AVC420、YUV420、8-bit provider；AVC444 模式额外要求本地
AVC420 decoder 作为有序双流基础，并注入 `Avc444DecoderProvider`，从而允许对服务器的
V10.7/AVC444 WireToSurface1 行为进行有界观察。凭据仍只从非终端 stdin 管道读取，环境
变量不承载凭据；默认不设上述变量时不会加载 backend、不会广告 EGFX。

本轮 continuation 提交 `fe82744` 增加了未知 FFmpeg capability bit 的 fail-closed 回归测试，
并修正桌面 frame-response CSV 测试只能检查目标列、避免合法的其他字段值造成随机误报。
固定 Rust 1.96.0 下，`cargo build --locked --release`、
`cargo build --locked --no-default-features`、`cargo test --locked --workspace --quiet` 和
`tools/verify-rdp-egfx-local.sh`（现已把两个构建配置纳入阶梯，含 macOS arm64 原生 fixture）均通过；后者仍明确不读取
凭据、不执行 live RDP，并保留 Windows/Linux hosted package 与真实 EGFX/HEVC 互操作门禁。

本轮继续收紧了跨架构 package 与能力确认边界：macOS package workflow 的 staging 现在显式
复用架构专属 FFmpeg build root；Windows verifier 对 EXE 与三个 codec DLL 执行
`0x8664`/`0x014c`/`0xAA64` PE Machine 精确校验，并同步更新安装器内嵌 verifier hash；Linux
静态 bundle verifier 增加同一 owner UID、拒绝 group/other writable 文件和拒绝路径组件
symlink 的门禁。`EgfxAdapter` 的 AVC420/AVC444 确认还要求实际配置了基础 H.264 decoder，
避免只凭服务器 `CapabilitiesConfirm` 误报本地能力。上述修改均保持真实 RDP/live gate
关闭，未读取凭据。

probe 只输出本地 backend 能力、选定 codec、provider 是否附着、首帧时间、frame boundary、
baseline、patch、解码字节数和断开回收结果。例如启用后会输出
`local_capability=software_exact provider=attached server_confirmation=pending`；这行是
本地能力证据，不是服务器确认。当前公开的 `SurfaceUpdate` 合同没有携带 EGFX/legacy
来源标签，因此最终 frame 统计也不会冒充 EGFX 首帧或持续刷新证明；仍必须在授权目标上
执行真实 `CapabilitiesConfirm → ResetGraphics → AVC420 first frame → sustained refresh →
recovery` 观察，并将结果另行记录。本轮新增的 `egfx_frame_confirmed` 非敏感诊断位只有
在 generation-bound EGFX `FrameBoundary` 成功发布到 runtime 后才置位；它与
`egfx_confirmed` 分开，避免把服务端能力确认误当作已显示 EGFX 画面。

### EGFX channel advertisement fix

离线 wire 检查发现 pinned IronRDP connector 默认没有把
`SUPPORT_DYN_VC_GFX_PROTOCOL`（`RNS_UD_CS_SUPPORT_DYNVC_GFX_PROTOCOL`，`0x0100`）写入
`TS_UD_CS_CORE.earlyCapabilityFlags`；仅注册 `EgfxAdapter` 因而不足以让 Windows 打开
`Microsoft::Windows::RDS::Graphics` DVC。现在根目录通过
`third_party/ironrdp-connector-0.10.0` 保留一个最小、可审计的本地补丁：只有精确 decoder
provider 已注入时，`frd-protocol-rdp` 才把该位置上；legacy connector 的默认值仍为
`false`。`connector::tests::egfx_provider_emits_the_graphics_channel_capability_on_the_wire`
和 `legacy_connector_omits_the_graphics_channel_capability_on_the_wire` 会分别实际编码并
重新解析有/无 provider 的 MCS Connect Initial，验证 wire bit，而不是只检查内存中的配置字段。
该修复解决了客户端广告链路的已知缺口，但没有替代目标服务器的
`CapabilitiesConfirm → ResetGraphics → AVC420 first frame` 真实证据；当前授权 probe 尚未
重跑，因此生产 EGFX/AVC420 门禁仍保持关闭。由于 pinned IronRDP 的 decoder reset 接口不携带
尺寸，publisher 现在把服务器 `ResetGraphics` 与 decoder 建立时的 coded size 做精确比较；
尺寸不一致时在入队 runtime Reset 前禁用 EGFX，并保留 legacy Bitmap/RemoteFX surface，避免
远端服务端调整请求分辨率后出现 decoder 与 framebuffer 尺寸错配。

### EGFX 与 legacy surface 的代际边界

EGFX `ResetGraphics` 现在通过同一个 `RdpBaseline::begin_next_generation` 原子重绑
legacy Bitmap/RemoteFX 覆盖跟踪器，再向 runtime 发布新 generation；因此 EGFX reset 之后
到达的 legacy update 不会因仍持有旧 generation 而被误判为 stale。显示重激活提交新的
surface generation 时，客户端会 fail-closed 禁用仍绑定旧代际的 EGFX publisher/adapter，
等待下一次明确的 EGFX 初始化；期间 legacy Bitmap/RemoteFX 继续使用新的 surface。由于
pinned IronRDP 的 decoder `reset()` 没有错误返回，`EgfxH264Decoder::reset_checked` 会把
底层 reset 错误保留为 decoder failed 状态；pinned decoder client 的 health query 会在调用
handler 前停止 reset，adapter 随后禁用 EGFX，避免发布不可用的 reset。reset 失败后，即使
同一 DVC payload 还带有 CreateSurface/Map/Frame 等尾随 PDU，也会在 client 入口短路，
不会再改变 EGFX 状态或通知 handler。对应的代际同步、重激活禁用和 reset-failure 测试
包含在当前协议测试集中。

## 2026-09-08 opt-in probe 结果

使用当前 release `.app` 内的 FFmpeg 8.1.2 bundle 运行了显式 AVC420 opt-in probe。非敏感
结果为：`backend=ffmpeg-8.1.2-software local_capability=software_exact
provider=attached`。新增的非敏感 graphics observer 报告
`egfx_advertised=true egfx_confirmed=false egfx_frame_confirmed=false avc420=false avc444=false`；连接通过
TLS/CredSSP/NLA，协商桌面为 1280×720，首帧约 1325 ms，20 秒内观察到
`resets=1 frames=110 full_baselines=2 patches=110 decoded_bytes=28000256`，主动断开后
worker 正常回收。当前 `SurfaceUpdate` 仍不携带来源标签；结合只有一个 legacy generation
reset，不能把这些帧计为 EGFX AVC420 帧。新增的 `egfx_frame_confirmed` 仍为 false；该目标本次没有提供
`CapabilitiesConfirm → ResetGraphics → AVC420 first frame` 的可审计证据，所以真实
AVC420 首帧门禁、AVC444 接线和 HEVC 首选均保持关闭；生产组合根仍默认 `LegacyOnly`，不广告 AVC420；Bitmap/RemoteFX 回退的成功结果与此前 legacy-only 观察一致。

## 已通过的本机检查

| 范围 | 结果 | 证据 |
|---|---|---|
| RGBA→BGRX 像素拷贝 | 已验证 | `frd-protocol-rdp` 的 `pixel_convert` parity/benchmark 测试；本机 Apple arm64 4×1920×1080 记录为 reference 228.5 MiB/s、NEON dispatch 731.1 MiB/s（3.20×，单次机器测量）；x86 使用 SSSE3 runtime gate，标量实现只作参考/不支持 CPU 回退。可用 `FRD_PIXEL_CONVERT_MIN_SPEEDUP=<正数>` 将显式性能门槛接入同一 benchmark，默认不把单机噪声写死进 CI |
| EGFX SurfaceUpdate 队列与 AVC444 参考层 | 已验证 | `cargo test --locked -p frd-protocol-rdp --quiet`：185 项通过；覆盖 Reset、完整 baseline、Damage、FrameBoundary、generation、边界拒绝、解码器尺寸不一致时的 EGFX fail-closed、架构 kernel 选择、RGBA→BGRX、YUV420→RGBA 与 YUV444→RGBA dispatch parity、短尾 SIMD parity、lazy SPS/PPS 提取、decoder 失败隔离，以及 AVC444 wire envelope、显式 decoder 分发和 YUV420→YUV444 V1/V2 参考重建的事务性/边界测试。新增有/无 EGFX provider 的 Client GCC wire-bit 重解析测试；`Avc444DecoderProvider` 只作为构造期、精确尺寸的双流 provider：两个 AVC 子流顺序复用一个 YUV420 decoder，上层按 V1/V2 事务性重建；YUV444→RGBA 已接入 x86 SSE4.1/AArch64 NEON，AVC444 目标矩形现在直接从带 stride 的 YUV444 平面转换，避免完整帧 RGBA 临时分配；WireToSurface1 现在按两个子流的 `regionRects` 并集裁剪为独立 SurfaceUpdate patch，非零目的矩形的掩码外像素不会被发布。新增 active-session 陈旧 session/generation 拒绝、EGFX reset 后 legacy generation 重绑、重激活时禁用旧 EGFX publisher、decoder reset 在发布 runtime Reset 前的 health gate，以及能力确认与实际 EGFX FrameBoundary 分离的 `egfx_frame_confirmed` 测试；YUV 平面 stride 的长度计算现在使用 checked arithmetic，溢出会在转换边界 fail-closed，并保留 zero-height 非零宽度 plane 校验契约；生产组合根和真实服务器门禁仍未打开 |
| IronRDP EGFX AVC420 PDU 链 | 已验证 | 同一协议测试通过真实 pinned `GraphicsPipelineClient` 路径，并由 `Avc420DecoderProvider` 创建精确 `EgfxH264Decoder`：ZGFX 解包、CapabilitiesConfirm、ResetGraphics、CreateSurface、MapSurfaceToOutput、StartFrame、WireToSurface1(AVC420)、EndFrame/FrameAcknowledge，以及 YUV420→RGBA→BGRX 的最终 `SurfaceUpdate`。适配层现在同时接受 [MS-RDPEGFX 规定的 Annex-B access unit](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-rdpegfx/5f12c20e-2ea1-4ad1-a2a0-019ee3893731) 和 pinned decoder 使用的四字节 length-prefix，并在一次边界归一化后交给 codec-neutral backend；对应的 IDR/parameter-set extraction 也使用归一化结果。pinned `ironrdp-egfx` 本地补丁现在按 [RDPGFX_RECT16 的排他坐标定义](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-rdpegfx/fe58f019-c3ee-4ca7-978e-7c9a860f9c3c) 解析并校验 surface-coordinate `regionRects` mask，以 `destRect` bounding frame 为坐标原点裁剪并逐区域发布 update；空或反向排他矩形在 PDU 解码边界直接拒绝，避免无效几何进入 payload 分发或发布路径。该 pinned crate 的独立离线测试也纳入本地阶梯（19 项通过）；AVC444 重建层在校验边界一次性转换为内部 inclusive 表示；quant/quality metadata 仍只保留为协议元数据，不参与解码 |
| EGFX decoder 注入边界 | 受限验证 | `RdpProtocolFactory::with_egfx_decoder_provider`、`Avc420DecoderProvider` 和构造期 `Avc444DecoderProvider` 可由应用组合根注入精确 media factory 与 SPS/PPS；新增 `Avc420DecoderProvider::from_factory` 可从首个完整 AVC access unit 提取 SPS/PPS 后再创建 decoder，AVC444 provider 在每个 surface 上要求协商尺寸与 coded size 精确一致并复用一个顺序 YUV420 decoder。Windows/macOS 组合根当前只准备 AVC420 provider，默认 `RdpGraphicsAdvertisementGate::LegacyOnly` 保持 `RdpProtocolFactory` legacy-only；固定 FFmpeg bundle 缺失或能力不匹配时使用 `RdpProtocolFactory::new`。只有显式 `LiveInteroperable` gate 的 bounded probe/已验证组合根才设置 `egfx_advertised`；`avc420`/`avc444` 保持 false，活动会话只在服务器 `CapabilitiesConfirm` 确认后更新非秘密诊断；AVC444 仍不由默认生产路径广告。若 pinned decoder 在 DVC 处理期间失败，adapter 会清空 EGFX surface queue、永久禁用该 EGFX stream，并让外围 ActiveStage 继续处理 legacy 输出；YUV420→RGBA 与 YUV444→RGBA 已接入 AArch64 NEON/x86 SSE4.1 leaf kernel，标量只在不支持目标 CPU 时回退，x86 目标和真实服务器首帧、持续刷新、降级与恢复行为仍需 live 验证 |
| H.264 AVC420 media contract | 已验证 | `cargo test --locked -p frd-media-api --quiet`：32 项通过 |
| FFmpeg H.264 ABI/AVC length-prefix bridge | 已验证 | `cargo test --locked -p frd-video-ffmpeg --quiet`：32 项通过；新增 2x2 SPS/PPS/IDR AVC420 fixture（SHA-256 `b7962d9f027d8539af68a2866539a56847af42fa2f73671d64036c7db239a1d7`）并验证 length-prefix→Annex-B 单次转换；`supports_avc420` 精确能力门禁覆盖 HEVC-only plugin 拒绝；`supports_hevc_main4448()` 只读探针覆盖 H.264-only plugin 拒绝，不会启用 RDP HEVC；native plugin capability 还检查 FFmpeg decoder profile 列表和 `AV_CODEC_CONFIG_PIX_FORMAT` 声明，实际 frame-format 再次校验；原生 FFmpeg plugin crate：4 项通过；C bridge 另以 `cc -std=c11 -Wall -Wextra -Werror -fsyntax-only` 对本机头文件通过，未冒充 FFmpeg 8.1.2 目标包验证 |
| Native FFmpeg HEVC/Main444, AVC420 and AVC444 fixtures | 受限验证 | `main444_decode` 已抽象为 Windows/macOS/Linux 动态库加载边界；macOS arm64 使用新构建的 FFmpeg 8.1.2 bundle 完成 3 项 native HEVC fixture（16×16 Main444 IDR、裁剪平面、非法参数 fail-closed）、1 项 2×2 visible-crop AVC420 fixture（16×16 coded surface、YUV420P8 planes）、1 项 16×16 AVC444 fixture（YUV444P8 planes）和 1 项 decoder-declared output capability fixture；2026-09-08 又以固定 Rust 1.96.0 `x86_64-apple-darwin` target 和 Rosetta 执行同一 6 项 fixture，全部通过，并验证 x86_64 bundle 的动态加载。Windows 原有 `LoadLibraryW` 路径保持不变；Linux x86_64 native host 在 bundle 构建阶段具备同一 HEVC/AVC fixture hook，i686/AArch64 仍只做交叉静态 gate |
| 桌面壳层回归 | 已验证 | `cargo test --locked -p frd-shell-desktop --quiet`：209 项通过；测试中的预期 decoder boundary panic 输出不代表失败 |
| AVC444 FFmpeg/backend、wire envelope 与参考重建 | 受限验证 | `H264Avc444`、YUV444P8 精确匹配测试通过；native plugin 已按 H.264/YUV444P8 建立独立 FFmpeg decoder entrypoint、报告 capability，并通过 16×16 AVC444 native fixture。`validate_avc444_bitmap` 按 pinned IronRDP 的 `Avc444BitmapStream` 解析 RFX_AVC444_BITMAP_STREAM，覆盖 LUMA_AND_CHROMA/LUMA/CHROMA、保留编码值、长度、trailing-byte 拒绝和拥有化区域列表；`Avc444Decoder` 分发边界在显式安装 decoder 时会校验 WireToSurface1、接收精确尺寸 RGBA、复用 BGRX/SurfaceUpdate 队列，并在解码错误时隔离 EGFX；`Avc444DecoderProvider` 已能构造一个顺序复用 AVC420 decoder 的 V1/V2 provider，YUV444→RGBA 使用 x86 SSE4.1/AArch64 NEON，尺寸不精确时 fail-closed。RDP wire profile 的服务器确认、生产组合根接线、完整色彩/区域语义和真实互操作仍未完成，因此默认仍 fail-closed、未广告/选择。线格式依据 [MS-RDPEGFX RFX_AVC444_BITMAP_STREAM](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-rdpegfx/844018a5-d717-4bc9-bddb-8b4d6be5dd3f)，双流组合依据 [YUV420p Stream Combination for YUV444](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-rdpegfx/8131c1bc-1af8-4907-a05a-f72f4581160f) 和 [YUV420p Stream Combination for YUV444v2](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-rdpegfx/781406c3-5e24-4f2b-b6ff-42b76bf64f6d) |
| Linux FFmpeg bundle path policy | 受限验证 | `rustc --edition=2021 --test crates/frd-video-ffmpeg/src/linux_bundle.rs` 的 3 个路径策略 fixture 通过（同 owner/权限、group writable 拒绝、symlink 拒绝）；Linux 构建脚本现在要求 Linux 主机/显式容器，跨架构会把目标 compiler/linker 传给 FFmpeg、Cargo 和 `cc`，x86 检查 `HAVE_X86ASM`、AArch64 检查 `HAVE_NEON`，并检查 ELF 架构和精确 bundle 文件集合；构建脚本按 Cargo 实际的绝对/相对 `CARGO_TARGET_DIR` 查找 plugin，隔离 target root 不会误取旧产物。native x86_64 host 可在临时 FFmpeg dist 仍存在时通过 `FRD_FFMPEG_RUN_NATIVE_TESTS=1` 跑 plugin 单测和 Main444 fixture；交叉架构不运行 hosted x86_64 测试。新增 `tools/verify-linux-ffmpeg-bundle.sh`，静态检查五文件集合、普通文件/无额外目录项、同一 owner UID、拒绝 group/other writable 文件、拒绝 bundle 路径组件 symlink、ELF Class/Data/Machine 精确匹配、SONAME、随包 `libavutil`/`libavcodec` 依赖、只允许独立 `$ORIGIN` RPATH 和 `frd_ffmpeg_get_api_v1` 导出；Linux x86_64 hosted job 还会以 `ctypes` 调用 ABI v1 入口并校验回调表，i686/arm64 只做静态 gate。真实 hosted job、Linux GUI package 与目标架构解码运行尚未执行 |
| HEVC selection ordering | 受限验证 | selector 单元测试覆盖四个独立缺失门禁和完整 `HEVC → AVC420 → AVC444 → RemoteFX → Bitmap` 顺序；FFmpeg loader 的 `supports_hevc_main4448()` 只报告本地 HEVC Main4448/YUV444P8 精确能力，不能替代 RDP wire 或 live 门禁；HEVC 只有 exact wire profile、exact decoder、live gate、当前会话协商四项同时成立才优先，当前仍未接入生产 connector。公开 MS-RDPEGFX `WireToSurface1` codec ID 表没有 HEVC 编号，Azure 的产品级 HEVC 说明没有给出可互操作的 RDP wire profile，因此 HEVC wire profile 仍为 `计划中`，不能提前广告或选择 |
| Windows/Linux/macOS core/video/plugin cross-target checks | 受限验证 | 在固定 Rust 1.96.0 下，`x86_64-unknown-linux-gnu`、`i686-unknown-linux-gnu`、`aarch64-unknown-linux-gnu`、`x86_64-pc-windows-msvc`、`i686-pc-windows-msvc`、`aarch64-pc-windows-msvc`、`x86_64-apple-darwin` 和 `aarch64-apple-darwin` 均通过 `cargo check --locked`，覆盖 `frd-core`、`frd-frame`、`frd-media-api`、`frd-protocol-api`、`frd-session`、`frd-ui-model`、`frd-video-ffmpeg` 和不启用 native FFmpeg C ABI 的 `frd-video-ffmpeg-plugin`；这只证明协议中立 Rust 核心、loader 和 plugin crate 的目标条件编译，不包含 FFmpeg native bundle、GUI 链接、Windows SDK/CRT 或运行时证据 |
| credential-free local verification ladder | 受限验证 | `tools/verify-rdp-egfx-local.sh` 固定 Rust 1.96.0，串联 `cargo fmt --all -- --check`、Shell 语法、`frd-media-api`/`frd-video-ffmpeg`/`frd-video-ffmpeg-plugin`/`frd-protocol-rdp`/`frd-shell-desktop` focused tests、默认 `cargo build --locked --release`、`cargo build --locked --no-default-features`、pinned `ironrdp-egfx` 的 19 项离线测试、顶层与 `hpssview` CLI help、workspace tests、上述八个 target checks，以及本机存在时的 macOS package verifier；Linux 主机可通过 `FRD_LINUX_FFMPEG_BUNDLE` 与 `FRD_LINUX_FFMPEG_PROFILE` 显式接入 Linux ELF/ABI verifier。native fixture 的 `FFMPEG_DIR` 与 `FRD_FFMPEG_TEST_BUNDLE` 现在统一按仓库根解析相对路径，且未指定 `FRD_FFMPEG_TEST_TARGET` 时不会因 `set -u` 空数组退出；本机在提供 macOS arm64 bundle/app/native fixture 且 `FRD_VERIFY_STRICT=1` 下运行通过。缺少的 package/native fixture 显示为未执行，严格模式才作为失败；脚本不读取凭据、不运行 live RDP，不能替代服务器互操作门禁 |
| workspace compile | 已验证 | `cargo check --locked --workspace --all-targets` 在当前 macOS arm64 工作区通过；仅有既有平台条件 warning |
| macOS arm64/x86_64 RDP adapter/package | 已验证 | `cargo check --locked --target aarch64-apple-darwin -p frd-protocol-rdp` 通过；以固定 Rust 1.96.0 分别运行 arm64 与 Rosetta x86_64 的 `FRD_MACOS_ARCH=... bash tools/stage-macos-package.sh release`，两个 `.app` 的 FFmpeg bundle、ad-hoc 签名、架构、CLI help 与 package verifier 均通过 |

## 目标架构门禁

| 客户端目标 | Rust/FFmpeg 状态 | 当前结论 |
|---|---|---|
| macOS arm64 | 本机 Rust target 与 NEON 路径已编译；新的显式 arm64 builder/stager/verifier 生成 release `.app`，其中包含 H.264+HEVC FFmpeg 8.1.2 arm64 bundle，签名、`--verify-codec-bundle`、Mach-O 架构、package verifier、native plugin 11 项单测、3 项 Main444 fixture、1 项 AVC420 fixture 和 1 项 AVC444 fixture 均通过；opt-in provider 已在授权 RDP probe 中加载；2026-09-08 又以相对 `CARGO_TARGET_DIR=target/verify-final` 完成 debug `.app` staging 和 package verifier，确认隔离 target root 不会回写默认 `target/macos` | `受限验证`，目标未产生可审计 EGFX AVC420 surface，生产 codec 选择仍保持 fail-closed，legacy 回退为默认路径 |
| macOS x86_64 | 2026-09-08 本机安装 NASM 后，`tools/build-ffmpeg-macos.sh` 使用 x86asm 构建 FFmpeg 8.1.2，固定 Rust `x86_64-apple-darwin` target 构建 plugin，完成 `.app` staging、Mach-O/package verifier，并在 Rosetta 下运行 6 项 HEVC/AVC420/AVC444 native fixture；macOS workflow 现显式安装对应 Rust target，macOS-15-intel hosted workflow 仍未执行，GUI 真机仍未验收 | `受限验证` |
| Windows x86_64 | 固定 Rust 1.96.0 的协议中立 core/video/plugin crates 已通过 `x86_64-pc-windows-msvc` target check；Windows workflow 现显式安装并检查该 target；Windows package verifier 现在还按 `0x8664` 精确校验 EXE 与三个 codec DLL 的 PE Machine，FFmpeg bridge 仍只完成现有 x86_64 package contract，本机完整 GUI/native check 仍因缺少 Windows SDK/CRT 在 `windows.h`/标准头处停止 | `开发中`，不可据此宣称可发布 |
| Windows x86/i686 | 固定 Rust 1.96.0 的协议中立 core/video/plugin crates 已通过 `i686-pc-windows-msvc` target check；Windows workflow 现显式安装并检查该 target；loader 与像素 kernel 有架构分支，Windows FFmpeg builder/stager/verifier 和 hosted workflow 已使用 `windows-x86` profile、i686 MSVC target、i686 MinGW 与 NASM/x86asm positive gate，verifier 按 `0x014c` 精确校验 EXE 与 codec DLL 的 PE Machine；本机缺少 Windows SDK，hosted package/native fixture 尚未通过 | `开发中` |
| Windows arm64 | 固定 Rust 1.96.0 的协议中立 core/video/plugin crates 已通过 `aarch64-pc-windows-msvc` target check；Windows workflow 现显式安装并检查该 target；loader 有架构目录分支，Windows FFmpeg builder/stager/verifier 和 hosted workflow 已使用 `windows-aarch64` profile、AArch64 MSVC target、AArch64 MinGW 与 FFmpeg NEON gate，verifier 按 `0xAA64` 精确校验 EXE 与 codec DLL 的 PE Machine；本机缺少 Windows SDK，hosted package/native fixture 尚未通过 | `开发中` |
| Linux x86/i686 | 固定 Rust 1.96.0 的协议中立 core crates 已通过 `i686-unknown-linux-gnu` target check；Linux FFmpeg 脚本包含 NASM/x86asm gate，loader 已接入固定 `linux-x86` bundle、同一 owner、非 group/other writable、无 symlink 路径门禁；workflow 现在还生成对应源码/hash evidence artifact，但 native bundle、package verifier 和 live gate 尚未执行 | `开发中` |
| Linux x86_64 | 固定 Rust 1.96.0 的协议中立 core crates 已通过 `x86_64-unknown-linux-gnu` target check；Linux FFmpeg 脚本包含 NASM/x86asm gate，loader 已接入固定 `linux-x86_64` bundle 和依赖先以 `RTLD_GLOBAL` 加载的安全路径；workflow 现在还生成对应源码/hash evidence artifact；native bundle、GUI package 和 live RDP gate 尚未执行 | `开发中` |
| Linux arm64 | 固定 Rust 1.96.0 的协议中立 core crates 已通过 `aarch64-unknown-linux-gnu` target check；Linux FFmpeg 脚本选择 AArch64/NEON 路径，loader 已接入固定 `linux-aarch64` bundle 门禁；workflow 现在还生成对应源码/hash evidence artifact；cross compiler、bundle 和 live gate 尚未执行 | `开发中` |

本机尝试的完整 Linux workspace target check 因缺少 `x86_64-linux-gnu-gcc`、Linux
sysroot 和 zlib 交叉链接输入而停止；上表的 core/video/plugin check 不依赖这些 native
输入。Windows 完整 GUI/native check 仍因缺少 Windows SDK/CRT 头文件失败。这些是明确的
unavailable gate，不会被记录为目标架构 package 或运行时通过。

## 尚未打开的生产门禁

2026-09-08 重新核对 Microsoft 的公开资料后，`RDPGFX_WIRE_TO_SURFACE_PDU_1` 仍只列出公开的 AVC/RemoteFX/Planar 等 codec ID；Azure Virtual Desktop 文档将 HEVC 描述为特定 GPU/全屏编码产品路径，并未给出可供本客户端复用的 RDPGFX codec ID、能力结构或 payload profile。因此本地 FFmpeg HEVC fixture 和 Azure 产品说明仍不能打开本客户端的 HEVC wire 门禁。

1. `baseline_connector` 只有在组合根提供精确 decoder provider、能创建 EGFX decoder，
   且显式传入 `RdpGraphicsAdvertisementGate::LiveInteroperable` 时才注册 `EgfxAdapter`；
   默认 `RdpProtocolFactory::with_egfx_decoder_provider` 仍保持 legacy-only。没有 provider
   或没有 live gate 时，`RdpGraphicsCapability` 中 `egfx_advertised`、`egfx_confirmed`、
   `avc420`、`avc444` 继续为 `false`。当前 Windows/macOS 的 bundle 加载失败路径也回退到
   该默认状态。
2. 产品级应用已经提供固定 FFmpeg bundle 的 AVC420 provider 注入，但尚未完成真实
   `CapabilitiesConfirm → ResetGraphics → AVC420 first frame → sustained refresh →
   recovery` 测试。协议层 provider 失败时会禁用 EGFX stream 并让外围 ActiveStage
   继续 legacy 处理；服务器在已经收到 EGFX 广告后是否发送可用 legacy 更新仍需 live
   证据。YUV420→RGBA 已有 AArch64 NEON/x86 SSE4.1 leaf kernel 和本机 parity 测试；
   Windows/Linux 目标编译、性能门槛和 bundle 级验证仍未通过。
3. AVC444 的 RDP wire profile 尚未由服务器证据确认；HEVC 的 RDP wire profile 和
   所有目标平台的真实生产互操作也尚未确认。因此 selector 只能作为 fail-closed
   规划接口存在，不能改变当前连接路径。
4. 没有把 Bitmap/RemoteFX 回退移除；没有 EGFX 或 decoder 失败时必须继续使用现有
   legacy surface path。

后续只有在逐目标 package、原生 decoder fixture、真实 EGFX 互操作和恢复测试都通过
后，才可以把对应 codec 从 `开发中` 提升到 `受限验证` 或 `已验证`。

补充：`cargo test --locked --workspace --quiet` 通过；完整 workspace 现包含 209 项 RDP
EGFX 测试，且没有修改提交的图标资产。`frd-icon-assets` 验证对 PNG 使用解码后的像素
和严格的单级跨架构 Lanczos 舍入预算，ICO 与原始 RGBA 仍按字节精确比较；这避免把
arm64/x86 浮点舍入差异误报成资产漂移。workspace 仍保留既有编译 warning，但没有测试失败。
